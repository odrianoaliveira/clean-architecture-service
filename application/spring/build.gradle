apply plugin: 'java-library'

ext.moduleName = 'adriano.cleanarchitecture.main'

dependencies {
    implementation(
            project(':config'),
            project(':domain'),
            project(':usecase')
    )
}